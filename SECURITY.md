# 安全政策 · Security Policy

## 支持的版本

我们仅对最新 `main` 分支提供安全更新。

| 版本 | 安全维护 |
|------|----------|
| `main` | ✅ |

## 报告漏洞

**请勿在公开 Issue 中披露安全漏洞。**

请通过以下任一私域渠道报告：

- **GitHub 私有漏洞报告（推荐）**：进入仓库 `Security → Report a Vulnerability`，全程私密。
- **Gitee 私有 Issue**：在 Gitee 仓库以「私密」方式提交。

我们会在一个工作日内确认收到，并在 7 个工作日内给出初步处置方案。报告时请尽量提供：复现步骤、影响范围、PoC（如有）。

## 安全设计要点（供审计参考）

- JWT 认证 + bcrypt 密码哈希 + 角色权限（admin / member）
- 全链路审计日志（自动记录 IP / UA）
- 速率限制、Helmet 安全头、Zod 输入校验
- 多租户行级隔离（`tenantId` 强制过滤）
- 敏感配置（如 `JWT_SECRET`）仅经环境变量注入，不进仓库

> ⚠️ 默认演示账号 `test@test.com / 123456` 仅供本地开发，生产部署请立即修改密码并配置强 `JWT_SECRET`。
